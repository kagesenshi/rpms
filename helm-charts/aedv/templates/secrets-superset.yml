apiVersion: v1
kind: Secret
metadata:
  name: {{ include "aedv.name" . }}-secrets
  labels:
    {{- include "aedv.labels" . | nindent 4 }}
type: Opaque
stringData:
  secret-key: {{ quote .Values.superset.secret_key }}
